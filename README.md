# Joplin Clipper Plus

Readability(Mozilla) 기반의 "간소화된 기사" 캡처를 핵심으로 한 조플린용 웹 클리퍼 크롬 확장(Manifest V3)입니다.

## 기능
- 📄 **Article** — Readability로 본문만 추출해 마크다운으로 저장 (광고/사이드바 제거)
- 🖼 **Full Page** — 스크립트를 제거한 전체 페이지 HTML 스냅샷 저장
- ✂️ **Selection** — 드래그로 선택한 영역만 마크다운으로 저장
- 🔖 **Bookmark** — 링크 + 설명만 가볍게 저장
- 노트북 선택, 태그 입력, 제목 수정 지원
- 본문에 박힌 유튜브 임베드 영상은 조플린이 자동 재생 임베드하는 형태로 변환해서 저장 (Article/Selection 모드)
- 사이트 자체 호스팅 동영상(mp4 등, 움짤 형식 포함)도 재생 가능한 `<video>` 태그로 저장 (Article/Selection 모드)

## 설치 방법

### 1. 조플린에서 Web Clipper 활성화
1. 조플린 데스크톱 앱 실행
2. **도구 → 옵션 → Web Clipper**
3. "Web Clipper 서비스 활성화" 켜기
4. 표시되는 **Authorization token**을 복사

### 2. 확장 프로그램 로드
1. Chrome/Edge 주소창에 `chrome://extensions` (Edge는 `edge://extensions`) 입력
2. 우측 상단 "개발자 모드" 켜기
3. "압축해제된 확장 프로그램을 로드합니다" 클릭
4. 이 폴더(`joplin-clipper`)를 선택

### 3. 토큰 등록
1. 확장 아이콘 클릭 → 팝업 상단의 "settings" 클릭 (또는 확장 관리 화면에서 "세부정보 → 확장 프로그램 옵션")
2. 포트(기본 41184)와 1단계에서 복사한 토큰 입력 → **Save**
3. **Test connection**으로 연결 확인 (조플린 앱이 켜져 있어야 함)

## 사용법
1. 클리핑하고 싶은 페이지로 이동
2. 확장 아이콘 클릭
3. 캡처 모드 선택 (Article / Full Page / Selection / Bookmark)
4. 제목 수정, 노트북 선택, 태그 입력 (선택)
5. **Clip to Joplin** 클릭

## 알려진 제한사항
- 이미지는 클리핑 시점에 1차로 `fetch`(올바른 Referer 자동 포함)로 받아 base64로 박아 넣습니다. `fetch` 자체가 CORS로 막혀 실패하면, 2차로 그 이미지를 화면에 스크롤해 보이게 한 뒤 **탭 스크린샷을 찍어 해당 영역만 잘라내는 방식**으로 캡처합니다. 이미지가 화면 세로 길이보다 크면 탭을 일시적으로 축소(zoom out)해서 통째로 한 번에 담은 뒤 원래 배율로 복구합니다(매우 긴 이미지는 화질이 다소 낮아질 수 있음). **이 2차 폴백은 [content.js](content.js)의 `SCREENSHOT_FALLBACK_HOSTS`에 등록된 사이트(현재 디시인사이드만)에서만 시도됩니다** — 다른 사이트에서 fetch가 실패하면 그냥 원본 URL만 남기고 넘어갑니다. 필요 없이 시도하면 캡처 시점에 화면에 겹쳐있던 페이지 UI가 이미지에 섞여 들어갈 수 있어서, 실제로 CORS 차단이 확인된 사이트만 화이트리스트로 관리합니다. 다른 사이트에서도 이 폴백이 필요해지면 같은 파일의 `SCREENSHOT_FALLBACK_HOSTS`에 호스트명을 추가하면 됩니다. 이 2차 폴백은 탭이 실제로 화면에 보이고 포커스된 상태여야 동작하고(클리핑 중 다른 창으로 전환하면 실패할 수 있음), 클리핑 한 번에 최대 20장까지만 적용됩니다(캡처 API 자체 호출 빈도 제한 때문). 두 방법 다 실패하면 원본 URL만 남습니다.
- 이미지가 3장 이상인 게시글은 지연 로딩(`data-original`/`data-src`)으로 전환되는 사이트가 있어, 해당 속성도 확인합니다. 화면 밖에 있던 이미지는 스크롤해서 실제로 로드될 때까지 최대 4초 대기 후 캡처합니다 — 네트워크가 느리면 그래도 빈 이미지가 캡처될 수 있습니다.
- 이미지 1장당 6MB 이상이면(스크린샷 폴백 제외) 노트 용량 문제로 건너뜁니다(원본 URL만 남음).
- 로컬 파일(`file://`), 크롬 내부 페이지(`chrome://`)에는 스크립트를 주입할 수 없어 클리핑이 되지 않습니다.
- 조플린 데스크톱 앱이 실행 중이어야 합니다 (Web Clipper 서비스가 앱 내부에서 로컬 서버로 돌아가는 방식).
- Article 모드는 게시판형 사이트에서 종종 엉뚱한 블록(사이드바, 목록 표)을 본문으로 잘못 판단할 수 있습니다. 그럴 땐 [content.js](content.js)의 `SITE_CONTENT_SELECTORS`에 `호스트명: "CSS 셀렉터"`를 추가하면 해당 사이트는 Readability를 건너뛰고 그 셀렉터에 매치되는 요소를 모두(제목/작성자/본문이 서로 다른 요소로 나뉘어 있으면 쉼표로 나열, 예: `".post_title, .post_author, .post_view"`) 문서 순서대로 합쳐서 뽑아옵니다 (디시인사이드, 클리앙이 이 방식으로 처리됨). 그 컨테이너 안에 여전히 원치 않는 위젯(추천 버튼, 첨부파일 목록 등)이 딸려오면 같은 파일의 `SITE_CLEANUP_SELECTORS`에 `호스트명: ["CSS 셀렉터", ...]` 형태로 추가하면 그 하위 요소들만 추가로 제거됩니다.
- 네이버 블로그처럼 실제 본문이 동일 출처 iframe 안에서 로드되는 사이트는 `SITE_IFRAME_SELECTORS`에 `호스트명: "iframe CSS 셀렉터"`를 추가하면 그 iframe 문서를 대신 분석합니다(Article 모드만 적용, Full Page/Bookmark 모드는 아직 미적용). Full Page 모드는 iframe 내용을 flatten하지 못해 이런 사이트에서는 여전히 빈 스냅샷이 나올 수 있습니다.

## 변경 이력
- **0.20.1** — **중요 버그 수정**: 같은 페이지를 새로고침 없이 두 번째 클리핑하면 "Clipping..." 상태에서 영영 멈춰버리던 문제. `content.js`가 클릭할 때마다 탭에 반복 주입되는데, 크롬의 "격리된 월드(isolated world)"가 탭 안에서 계속 유지되다 보니 두 번째 주입부터 `const SITE_CONTENT_SELECTORS` 같은 최상위 선언이 "이미 선언됨" 에러로 스크립트 전체를 조용히 멈춰버렸던 것. `content.js` 전체를 즉시실행함수(IIFE)로 감싸서 매 주입마다 독립된 스코프를 갖도록 수정 — Node로 "같은 스크립트 두 번 주입" 상황을 직접 재현해서 수정 전/후 검증함. 추가로 이미지 다운로드에 12초, 클리핑 전체에 45초 제한 시간을 걸어서, 앞으로 비슷하게 알 수 없는 이유로 멈추는 경우에도 최소한 오류 메시지는 뜨도록 안전장치 추가.
- **0.20.0** — 아카라이브(arca.live) 클리핑 지원 추가. 게시글 목록 표, 사이드바 등이 통째로 딸려오던 문제 — `SITE_CONTENT_SELECTORS`에 `.fr-view.article-content` 등록해서 해결(⚠️ 이 사이트는 브라우저 안전 정책상 직접 접속이 막혀서 사용자가 보내준 페이지 소스로 확인함, 실제 클리핑 동작 확인 필요). 아카라이브 이미지의 `data-originalurl` 속성(고화질 원본)도 인식하도록 `jcpRealImgUrl`에 추가 — 다른 사이트에도 범용 적용됨.
- **0.19.0** — 인벤(inven.co.kr) 클리핑 지원 추가. 인벤 고유 기능인 "인벤토리" 위젯(사용자 뱃지/아이템 표시)이랑 추천 버튼 블록이 같이 딸려오던 문제 — `SITE_CONTENT_SELECTORS`에 `.articleTitle h1, #powerbbsContent` 등록해서 해결. 이미지 CORS도 막혀있어 `SCREENSHOT_FALLBACK_HOSTS`에도 추가.
- **0.18.1** — 네이버 블로그에서 사진이 여러 장일 때 일부만 원본 크기로 잡히고 나머지는 목록 썸네일 수준으로 작게 잡히던 문제 수정. 스마트에디터 ONE은 화면에 아직 안 보인 이미지를 `data-lazy-src` 속성에 원본 주소를 숨겨두고 `src`엔 작은 미리보기만 남겨두는데, 이 속성을 확인 안 하고 있었음(범용 개선, `jcpRealImgUrl`/`jcpInlineImages`에 `data-lazy-src` 지원 추가). ⚠️ 역시 라이브 테스트는 못 함, 확인 필요.
- **0.18.0** — 이토랜드(etoland.co.kr) 클리핑 지원 추가. 댓글/다른글목록/카테고리 필터가 통째로 딸려오던 문제 — `SITE_CONTENT_SELECTORS`에 `article h1, .view-content` 등록. 이 사이트 동영상은 `src`가 아니라 `data-src`에 실제 주소가 있는 지연로딩 방식이라 동영상이 아예 빠지던 문제도 발견 — `jcpCleanVideoTags`가 이제 `data-src`도 확인함(범용 개선, 다른 사이트에도 적용됨). 영상 재생 컨트롤 UI(시간/배속 표시) 텍스트 찌꺼기는 `SITE_CLEANUP_SELECTORS`로 제거.
- **0.17.0** — SLR클럽(slrclub.com) 클리핑 지원 추가. 댓글/광고/이전글다음글 등이 같이 딸려오던 문제 — `SITE_CONTENT_SELECTORS`에 `#userct` 등록해서 해결. 이미지 CORS도 막혀있어 `SCREENSHOT_FALLBACK_HOSTS`에도 추가.
- **0.16.0** — 흥미유발닷컴(web.humoruniv.com) 클리핑 지원 추가. 댓글 전체(5만자 이상)를 본문으로 잘못 긁어오고, 첨부 이미지는 CORS로 막혀 안 나오던 문제 — `SITE_CONTENT_SELECTORS`에 `#cnts` 등록하고 `SCREENSHOT_FALLBACK_HOSTS`에도 추가해서 해결.
- **0.15.0** — 유튜브가 아니라 **사이트 자체 호스팅 동영상**(`<video>` 태그, mp4 직링크 — 아아갓의 "움짤" 형식 등)이 통째로 빠지던 문제 해결. 원본 동영상 URL을 가리키는 깔끔한 `<video controls>` 태그로 변환해서 조플린에서 바로 재생되게 함(`jcpCleanVideoTags`). 용량이 커서 이미지처럼 base64로 내장하진 않고 원본 URL을 그대로 참조. 사이트마다 커스텀 재생 컨트롤 UI(속도조절, 프로그레스바 등)가 같이 붙어있으면 `SITE_CLEANUP_SELECTORS`로 제거(아아갓 `.s_opt`, `.v_progress` 적용).
- **0.14.1** — 네이버 블로그 본문 위에 "공지 목록" 위젯이 같이 딸려오던 문제 수정. `SITE_CONTENT_SELECTORS`에 `.se-main-container`(스마트에디터 ONE의 본문 고정 클래스) 등록. ⚠️ 이것도 라이브 테스트는 못 하고 일반적으로 알려진 구조로 구현함.
- **0.14.0** — 네이버 블로그(blog.naver.com) Article 모드 클리핑이 "파서 실패"로 아예 안 되던 문제 수정. 네이버 블로그는 실제 글 내용이 최상위 페이지가 아니라 `#mainFrame`이라는 iframe 안에서 로드되는데, 클리퍼는 최상위 페이지만 보고 있어서 본문 자체를 못 찾았던 것. `SITE_IFRAME_SELECTORS`(신규)로 동일 출처 iframe을 감지해서 그 iframe의 문서를 대신 분석하도록 수정(`jcpGetEffectiveDoc`). ⚠️ 브라우저 안전 정책상 blog.naver.com에 직접 접속해 테스트하지 못했음 — 일반적으로 알려진 구조를 바탕으로 구현, 실제 동작 확인 필요.
- **0.13.0** — 아아갓(aagag.com) 클리핑 지원 추가. 클립/신고/정치 카테고리 버튼, 데이터 절약량 위젯, 여러 사이트 댓글 통계, "다른 이슈" 목록 표까지 통째로 딸려오던 문제 — `SITE_CONTENT_SELECTORS`에 `h1.title, #vContent`(제목+본문만) 등록. 이미지 CDN도 CORS가 막혀있어 `SCREENSHOT_FALLBACK_HOSTS`에도 추가.
- **0.12.0** — 뀨잉넷(qquing.net) 클리핑 지원 추가. 실제 본문(웹코믹 이미지 1장)이 아니라 광고성 카테고리 링크 + 이전글/다음글/목록 네비게이션 + 댓글/관련자료까지 통째로 긁어오던 문제 — `SITE_CONTENT_SELECTORS`에 `#bo_v_con` 등록해서 해결.
- **0.11.1** — 클리앙 클리핑 시 댓글/공감 버튼이 "2", "4" 같은 라벨 없는 숫자로만 나오던 문제 수정. 실제 라벨("댓글 2개", "공감 6명")은 화면엔 안 보이고 `title` 툴팁 속성에만 있었는데, 이제 그 속성값을 꺼내서 눈에 보이는 텍스트로 바꿔 넣음. `SITE_LABEL_FROM_TITLE_SELECTORS`라는 새 사이트별 설정 추가 — 아이콘+숫자만 있고 진짜 라벨이 title 속성에 숨어있는 사이트에 재사용 가능.
- **0.11.0** — 본문에 박힌 유튜브 임베드 영상이 클리핑 시 통째로 빠지던 문제 해결. 이제 유튜브 iframe을 찾아서 "다른 텍스트 없이 그 줄에 URL만 있는" 형태(`https://www.youtube.com/watch?v=ID`)로 바꿔 넣는데, 이게 정확히 조플린이 영상으로 자동 임베드해주는 조건이라 노트를 열면 영상이 재생됩니다. (마크다운 링크 문법으로 감싸거나 다른 글자와 같은 줄에 있으면 조플린이 자동 임베드를 안 해줘서, 줄바꿈으로 완전히 분리시켜 넣도록 구현.)
- **0.10.0** — 오늘의유머(todayhumor.co.kr) 클리핑 시 게시물ID·IP·조회수·추천한 분들 목록·범례 텍스트 같은 메타/푸터 정보가 본문에 섞여 들어오던 문제 수정. `SITE_CONTENT_SELECTORS`에 `.viewSubjectDiv, .viewContent`(제목+본문만) 등록.
- **0.9.1** — 오늘의유머(todayhumor.co.kr)에서 이미지 클리핑 시 "Mixed Content" 콘솔 오류 발생하던 문제 수정. 해당 사이트 이미지 서버가 https 자체를 지원하지 않아(https로 시도해도 실패 확인) HTTPS 페이지에서 fetch를 시도하면 브라우저가 무조건 차단하며 경고를 남기는데, 이제 그런 조합이면 fetch 시도 자체를 건너뛰고 바로 스크린샷 폴백으로 넘어감. `SCREENSHOT_FALLBACK_HOSTS`에 todayhumor.co.kr 추가.
- **0.9.0** — 뽐뿌(ppomppu.co.kr) 클리핑 지원 추가. 이미지 위주 게시글(텍스트가 거의 없는 짤/밈 글)은 Readability가 본문 자체를 못 찾아 거의 빈 결과가 나오던 문제 — `SITE_CONTENT_SELECTORS`에 `.board-contents` 등록해서 해결. 이미지 CDN도 디시처럼 CORS가 막혀있어 `SCREENSHOT_FALLBACK_HOSTS`에도 추가.
- **0.8.0** — 클리앙(clien.net) 게시글 클리핑 시 게시글 목록 표(수십 개 글 제목/날짜/조회수)가 같이 딸려오던 문제 수정. 클리앙은 제목(`.post_title`)·작성자정보(`.post_author`)·본문(`.post_view`)이 서로 다른 요소라, `SITE_CONTENT_SELECTORS`가 셀렉터 하나만 처리하던 걸 **쉼표로 나열된 여러 요소를 모두 찾아 문서 순서대로 합치는 방식**으로 확장.
- **0.7.1** — 탭 스크린샷 이미지 캡처 폴백(줌아웃 후 캡처)을 모든 사이트가 아니라 **디시인사이드에만 적용**하도록 제한. 다른 사이트(네이버 뉴스 등)는 이미지 CDN이 CORS를 막지 않는 경우가 많은데도 이 폴백이 시도되면서, 캡처 시점에 화면에 겹쳐있던 페이지 UI(뉴스 카테고리 라벨 등)가 이미지에 섞여 들어가는 문제가 있었음. `SCREENSHOT_FALLBACK_HOSTS`로 대상 사이트를 명시적으로 제한.
- **0.7.0** — 뉴스 통신사(머니투데이 등)가 기사 본문 HTML 안에 직접 심어두는 "[OO 주요뉴스]" 관련기사 목록 + "ⓒ ... 무단전재 재배포 금지" 저작권 문구 제거. 특정 사이트 셀렉터가 아니라 **범용 패턴 매칭**으로 처리해서(`jcpStripTrailingWireFooter`), 같은 통신사 기사를 퍼가는 다른 포털(네이버, 다음 등)에서도 자동으로 적용됨.
- **0.6.1** — 디시 게시글 본문에 딸려오던 푸터 위젯(추천/비추천, 실베추·펌·공유·스크랩·신고 버튼, 원본 첨부파일 목록, 관련 갤러리 박스, 광고) 제거. `SITE_CLEANUP_SELECTORS`라는 사이트별 추가 제거 목록을 새로 만들어서, `SITE_CONTENT_SELECTORS`로 잡은 컨테이너 안에서도 원치 않는 하위 요소를 더 걸러낼 수 있게 확장.
- **0.6.0** — 긴 이미지 스크린샷 캡처 방식을 여러 조각 이어붙이기(스티칭)에서 **탭을 일시적으로 축소(zoom out)해서 한 번에 통으로 캡처**하는 방식으로 교체. 스티칭 방식은 스크롤/캡처 사이 타이밍 오차로 이음새(가로줄)가 생기는 문제가 있었음 — 한 번에 찍으면 이음새 자체가 생길 수 없음. 대신 아주 긴 이미지는 해상도가 다소 낮아질 수 있음.
- **0.5.0** — 이미지 관련 2가지 버그 수정: (1) 게시글에 이미지가 여러 장(3장 이상)이면 지연 로딩(lazy-load)으로 전환되어 `src` 대신 `data-original`에 실제 주소가 들어있는데, 이를 확인 안 해서 화면 밖 이미지가 통째로 빠지던 문제 — `data-original`/`data-src`도 확인하도록 수정. (2) 세로로 긴 이미지(전신 사진 등)가 화면 한 번에 안 들어가 가운데 부분만 찍혀서 위/아래(얼굴 등)가 잘리던 문제 — 스크롤하며 여러 조각으로 나눠 찍은 뒤 이어붙이도록 수정.
- **0.4.0** — 이미지 CORS 완전 차단(fetch 자체가 "Failed to fetch"로 실패) 사이트 대응. `fetch` 실패 시 탭 스크린샷 API(`chrome.tabs.captureVisibleTab`)로 실제 페이지에 렌더링된 이미지 영역만 잘라내 캡처하는 2차 폴백 추가. 디시인사이드(`dcimg*.dcinside.co.kr`)에서 확인.
- **0.3.1** — Turndown이 `<script>`/`<style>` 태그 내용을 기본적으로 걸러내지 않아 디시 게시글 본문에 박혀있는 이미지 번호매기기/광고 로딩용 인라인 스크립트가 그대로 마크다운에 새어나오던 문제 수정. 변환 전 DOM에서 확실히 제거(`jcpStripNonContentTags`) + Turndown `remove()` 이중 방어.
- **0.3.0** — 사이트별 본문 셀렉터 오버라이드(`SITE_CONTENT_SELECTORS`) 추가. 디시인사이드(`gall.dcinside.com`)처럼 하단 게시글 목록이 실제 본문보다 텍스트가 훨씬 많아 Readability가 페이지 전체를 통째로 긁어오는 사이트에 대응 — `.view_content_wrap`을 직접 지정해서 본문만 정확히 추출.
- **0.2.0** — 이미지 인라인화(base64) 추가로 핫링크 방지 이미지(디시 등) 대응. 게시판 네비게이션/메타 찌꺼기 제거(`jcpStripBoardChrome`). 팝업에 버전 표시 추가.
- **0.1.0** — 최초 버전: Article/Full Page/Selection/Bookmark 캡처.

## 파일 구조
```
manifest.json     MV3 매니페스트
background.js     서비스 워커: 조플린 API 호출, 스크립트 주입 오케스트레이션
content.js        페이지에 주입되어 Readability/Turndown 실행
popup.html/js     클리핑 UI
options.html/js   토큰/포트 설정 화면
lib/              Readability.js, turndown.js, turndown-plugin-gfm.js (MIT/Apache-2.0)
```
